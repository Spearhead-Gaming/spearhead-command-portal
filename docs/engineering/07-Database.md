# Database Standards

- MariaDB
- Prisma
- Foreign keys
- Soft deletes where appropriate
- Audit critical actions
- Timestamp all entities
- Member profiles should use `displayName` as the primary visible identity, with optional `firstName` and `lastName` metadata and optional rank support
