# Qualification Matrix UX

The matrix should be unit-scoped by default and show required qualifications first.

Default filters: unit, position/billet, category, missing, overdue, expiring, and member status.

Cell states must not rely on color alone. A cell opens the qualification record inspector for award, edit, renew, revoke, or review.
